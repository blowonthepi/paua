# Pāua

## A Jetpack Compose architecture experiment

#### Something to note:

In the <code>tools</code> directory you'll find a <code>StateRouter</code> with an example in the documentation. This isn't implemented
anywhere in Pāua as yet, however it has been included to give an example of how to make the Router pattern remember each of it's child
screens state between recompositions.